type Query {
  events: [Event]
  event(id: Int!): Event
}

type Event {
  body: JSON
  created: Date
  end: Date
  id: Int
  image: Media
  owner: User
  place: JSON
  published: Boolean
  start: Date
  title: JSON
  updated: Date
}
