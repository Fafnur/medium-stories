import { FormHostDirective } from './form-host.directive';

describe('FormHostDirective', () => {
  it('should create an instance', () => {
    const directive = new FormHostDirective(null);
    expect(directive).toBeTruthy();
  });
});
