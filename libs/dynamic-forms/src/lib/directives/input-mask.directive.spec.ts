import { InputMaskDirective } from './input-mask.directive';

describe('FieldMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMaskDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
